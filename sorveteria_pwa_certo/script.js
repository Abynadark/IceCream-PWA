var swiper = new Swiper(".swiper", {
    cssMode: true,
    loop: true,
    pagination: {
        el: ".swiper-pagination",
    },
    keyboard: true,
    touch: true,       // Ativa o modo touch
    mousewheel: true,  // Ativa o suporte a roda do mouse
});
